# env / secrets
.env
.env.local

# python
__pycache__/
*.pyc
.venv/
tts/.venv/

# editor / OS / tooling
.idea/
.vscode/
.DS_Store
*~

# build / runtime
node_modules/
*.log

# project-specific
Junk/
webapp/assets/*~

# generated library cache (unsloth) — don't commit
unsloth_compiled_cache/

# raw Live2D source assets — kept locally, not in the repo
live2d_raw/

# heavy artifacts kept on disk but not in initial baseline
webapp/voice_index.bin

# deploy state (created at runtime)
data/
letsencrypt/
