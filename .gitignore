# env / secrets
.env
.env.local

# python
__pycache__/
*.pyc
.venv/
tts/.venv/

# editor / OS / tooling
.idea/
.vscode/
.DS_Store
*~

# build / runtime
node_modules/
*.log

# the bare metal Windows install, portable PHP, the TTS venv, models, chat, logs
runtime/

# project-specific
Junk/

# Game-ripped Live2D assets - rebuilt locally with tools/recover_assets.py,
# NOT redistributed with this repo (see the NOTICE in LICENSE). Kept out of
# git so a fork can't accidentally republish them.
webapp/assets/

# generated library cache (unsloth) - don't commit
unsloth_compiled_cache/

# dataset generation tools
tools/dataset_v5
tools/dataset_v6

# LoRA training pipeline and its debug scratch - operates on the gitignored
# dataset dirs, nothing in the running stack imports it. Not to be confused with
# colab.ipynb, which serves the app and stays tracked.
train_jun.py
train_think_colab.ipynb
tools/build_dataset_v6.py
diag_labels.py
diag_thinking.py
smoke_thinking.py
from_think_tags.py
webapp/api/review.php

# raw Live2D source assets - kept locally, not in the repo
live2d_raw/

# lore corpus - generated from tools/lore_dataset.jsonl by build_lore_index.php
# (.bin/.json came from the embedding index, deleted in 2026-07; still ignored so
# stale copies in older checkouts never get committed)
webapp/lore_corpus.txt
webapp/lore_index.bin
webapp/lore_meta.json


# deploy state (created at runtime)
/data/
letsencrypt/

# local .env snapshots kept while switching backends
.env.bak-*

# game archive (never republish - see LICENSE NOTICE)
factorial-omega-win-64.zip
