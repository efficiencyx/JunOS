# Screenshots

Drop the images referenced by the main `README.md` here:

| File | Used for |
|---|---|
| `hero.png` | Optional banner at the top of the README |
| `chat.png` | Chat interface with live reactions |
| `action.png` | Character mid-reaction |
| `demo.gif` | Short screen recording of a full exchange |

Keep them reasonably sized (a few hundred KB each); compress large PNGs before committing.
