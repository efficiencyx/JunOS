# Karaoke sidecar: the same server.py, minus the TTS engines. Stem separation
# (htdemucs) plus whisper word timestamps for the lyric track. Kept apart from
# requirements.txt so the voice image stays CPU-sized while this one is built
# against a CUDA/ROCm torch.
#
# demucs needs torch>=2.1 and does not pull torchaudio - that comes from the
# torch index in docker/karaoke.Dockerfile so its wheel build matches torch's.
#
# Pinned exactly for the same reason requirements.txt is, see the note there.
# torch keeps a range because TORCH_INDEX points at cpu, cu124 or rocm and they
# don't carry the same versions.
demucs==4.1.0
faster-whisper==1.2.1
torch>=2.5,<2.14
fastapi==0.141.1
uvicorn[standard]==0.52.1
soundfile==0.14.0
numpy==2.2.6
